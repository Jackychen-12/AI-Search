# Pin the public registry so lockfile "resolved" URLs never point at a private
# mirror — Vercel / GitHub Actions can't reach internal registries and the
# whole deploy fails at npm ci.
registry=https://registry.npmjs.org/
